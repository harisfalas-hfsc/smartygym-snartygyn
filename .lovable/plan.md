
User wants to install Google Ads / GA4 tracking tag (G-VCRTFQDY85) site-wide for verification.

Best place: `index.html` `<head>`. It's a standard global site tag, must load on every page, and the head is the recommended location per Google.

Need to check if any existing gtag/GTM is already there to avoid duplicates.
